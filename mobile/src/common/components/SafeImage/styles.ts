import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    // Base styles for the image
    // Specific dimensions should be set via style prop
  },
  fallbackContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
