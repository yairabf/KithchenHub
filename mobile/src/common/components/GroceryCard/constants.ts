/**
 * Constants for GroceryCard components
 */

/**
 * Default minimum quantity for quantity controls
 * Users cannot decrement below this value
 */
export const DEFAULT_MIN_QUANTITY = 1;

/**
 * Default image position for grocery card content
 * Can be 'left' to show image, or 'none' to hide
 */
export const DEFAULT_IMAGE_POSITION = 'left' as const;
