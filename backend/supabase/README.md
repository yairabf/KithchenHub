# Supabase Storage Configuration

This folder contains SQL scripts for configuring Supabase storage buckets and RLS policies.

## Setup Instructions

### Option 1: Via Supabase Dashboard (Recommended)

1. Go to your Supabase project: https://supabase.com/dashboard/project/dpjcavnhkifatwzezwgo
2. Navigate to **SQL Editor**
3. Copy the contents of `storage-policies.sql`
4. Paste and run the script

### Option 2: Via Supabase CLI

```bash
# Install Supabase CLI if you haven't
npm install -g supabase

# Link to your project
supabase link --project-ref dpjcavnhkifatwzezwgo

# Run the migration
supabase db execute --file backend/supabase/storage-policies.sql
```

### Option 3: Via psql

```bash
psql "postgresql://postgres.dpjcavnhkifatwzezwgo:YOUR_PASSWORD@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres" \
  -f backend/supabase/storage-policies.sql
```

## What This Sets Up

### 1. `public-assets` Bucket (Catalog Images)
- **Public:** Yes (no signed URLs needed)
- **Access:** 
  - ✅ Authenticated users can **read** catalog images
  - ✅ Service role (backend) can upload/update/delete
  - ❌ Anonymous users cannot access
- **File size limit:** 5MB
- **Allowed types:** JPEG, PNG, WebP, SVG
- **Example path:** `catalog-icons/tomato.png`

### 2. `household-uploads` Bucket (User Uploads)
- **Public:** No (signed URLs required)
- **Access:**
  - ✅ Users can **read** only their household's files
  - ✅ Service role (backend) can upload/update/delete
  - ❌ Users cannot access other households' files
- **File size limit:** 10MB
- **Allowed types:** JPEG, PNG, WebP, GIF
- **Path format:** `{householdId}/recipes/{recipeId}/{filename}`

## How It Works

### Catalog Images (public-assets)
- Backend uploads images using service role key
- Mobile/web fetch with authenticated user token
- Public bucket = no signed URLs needed (but RLS still enforces auth)

### User Uploads (household-uploads)
- Backend uploads images to `{householdId}/recipes/{recipeId}/` path
- Backend generates signed URLs for the user's household
- RLS ensures users can only access their household's folder
- Path structure enforces household isolation

## Security Notes

1. **Never expose service role key to clients** - it bypasses all RLS
2. **Backend uses service role** for uploads (full access)
3. **Clients use anon key** for downloads (RLS enforced)
4. **File paths encode household ID** for isolation
5. **Signed URLs** are generated by backend for private bucket access

## Verification

After running the script, verify with these queries in Supabase SQL Editor:

```sql
-- Check buckets
SELECT id, name, public, file_size_limit, allowed_mime_types 
FROM storage.buckets;

-- Check policies
SELECT schemaname, tablename, policyname, permissive, roles, cmd 
FROM pg_policies 
WHERE tablename = 'objects' AND schemaname = 'storage';
```

## Testing

### Test catalog-images access:
```bash
# Should work (authenticated user)
curl -H "Authorization: Bearer YOUR_USER_JWT" \
  https://dpjcavnhkifatwzezwgo.supabase.co/storage/v1/object/public/public-assets/catalog-icons/tomato.png

# Should fail (no auth)
curl https://dpjcavnhkifatwzezwgo.supabase.co/storage/v1/object/public/public-assets/catalog-icons/tomato.png
```

### Test household-uploads access:
```bash
# User can access their household's files
curl -H "Authorization: Bearer USER_JWT_WITH_HOUSEHOLD_123" \
  "https://dpjcavnhkifatwzezwgo.supabase.co/storage/v1/object/sign/household-uploads/household-123/recipes/recipe-1/image.jpg"

# User CANNOT access another household's files (will get 403)
curl -H "Authorization: Bearer USER_JWT_WITH_HOUSEHOLD_123" \
  "https://dpjcavnhkifatwzezwgo.supabase.co/storage/v1/object/sign/household-uploads/household-456/recipes/recipe-1/image.jpg"
```

## Current Backend Configuration

Your backend is already configured to use these buckets:

- **Storage adapter:** `SupabaseStorageAdapter` (uses service role)
- **Bucket:** `household-uploads` (from `STORAGE_BUCKET` env var)
- **Upload method:** `putObject()` - uploads with service role (bypasses RLS)
- **Signed URLs:** `getSignedUrl()` - generates time-limited access URLs

## Next Steps

After applying these policies:

1. ✅ Upload catalog images to `public-assets` bucket
2. ✅ Update `CATALOG_ICONS_BASE_URL` to point to public-assets
3. ✅ Verify recipe image uploads go to `household-uploads/{householdId}/recipes/...`
4. ✅ Test that users can only access their household's images

## Database Table RLS Remediation

This repository also enforces RLS for `public` schema tables via Prisma SQL migrations.

### Primary migrations

- `20260122130000_enable_rls_and_policies` (initial household/user RLS rollout)
- `20260401130000_rls_remaining_public_tables` (coverage for newer tables and sensitive tables)

### Tables covered by remediation migration

- Sensitive/user scoped: `refresh_tokens`, `sync_idempotency_keys`
- Household scoped: `household_invites`, `custom_items`
- Read-only audit visibility: `audit_logs` (read-only by household/user; no client write policy)
- Read-only catalog reference tables: `catalog_item_i18n`, `catalog_item_aliases`, `catalog_tags`, `catalog_item_tags`

### Verification queries (Supabase SQL Editor)

```sql
-- 1) Ensure RLS is enabled on public tables
SELECT schemaname, tablename, rowsecurity
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY tablename;

-- 2) Inspect policies for public schema
SELECT schemaname, tablename, policyname, permissive, roles, cmd
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

Expected outcomes:

- `rowsecurity = true` on all user-data tables in `public`
- no permissive client write policy on sensitive internal tables
- household/user scoped policies restrict cross-tenant access

### Security Advisor closeout checklist

1. Run migrations on target Supabase environment.
2. Re-run Security Advisor scan.
3. Confirm `rls_disabled_in_public` is cleared.
4. Confirm `sensitive_columns_exposed` is cleared.
5. If any warning remains, add a follow-up migration for the flagged table.
