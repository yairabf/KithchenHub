# =============================================================================
# Environment Configuration
# =============================================================================
NODE_ENV=development
PORT=3000

# =============================================================================
# Database Configuration (REQUIRED)
# =============================================================================
# For Docker Compose, these use the 'postgres' service hostname from docker-compose.yml
# The credentials match the PostgreSQL service environment variables in docker-compose.yml
DATABASE_URL=postgresql://kitchen_hub:kitchen_hub_dev@postgres:5432/kitchen_hub?schema=public

# DIRECT_URL is used by Prisma for migrations and direct database access
# For local Docker Compose, this is typically the same as DATABASE_URL
# For Supabase deployments, DIRECT_URL bypasses the connection pooler for migrations
DIRECT_URL=postgresql://kitchen_hub:kitchen_hub_dev@postgres:5432/kitchen_hub?schema=public

# =============================================================================
# JWT Authentication (REQUIRED)
# =============================================================================
# Generate secure random strings (minimum 32 characters):
#   openssl rand -base64 32
# Or use: https://randomkeygen.com/
# IMPORTANT: Use different secrets for JWT_SECRET and JWT_REFRESH_SECRET
JWT_SECRET=change-me-to-32+chars-minimum-length-required
JWT_REFRESH_SECRET=change-me-to-32+chars-minimum-length-required
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# Google OAuth (OPTIONAL - only if using Google sign-in)
# =============================================================================
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create OAuth 2.0 Client ID credentials
# 3. Application type: Web application
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Get these from your Supabase project dashboard:
# 1. Go to https://app.supabase.com/
# 2. Select your project
# 3. Go to Settings → API
# 4. Copy Project URL, anon key, and service_role key
# NOTE: SUPABASE_SERVICE_ROLE_KEY has admin access - keep it secret!
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# Catalog Icons Storage (OPTIONAL - for Docker with MinIO)
# =============================================================================
# Base URL for catalog icon images (e.g. MinIO bucket). When set, relative
# image_url values in the grocery catalog are rewritten to full URLs in API
# responses. Mobile app uses this URL to load icons. For local Docker, use
# the host-accessible MinIO URL (mobile runs on host).
CATALOG_ICONS_BASE_URL=http://localhost:9000/catalog-icons
