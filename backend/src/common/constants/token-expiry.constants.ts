/**
 * Token expiry constants for authentication tokens.
 */
export const REFRESH_TOKEN_EXPIRY_DAYS = 7;

/**
 * Maximum number of items allowed in a single sync operation.
 */
export const MAX_SYNC_ITEMS = 1000;
