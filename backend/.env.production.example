# =============================================================================
# Production Environment Variables
# =============================================================================
# ⚠️  NEVER commit this file with real values to git!
# Copy this file to .env.prod and fill in production values
# =============================================================================

# Environment
NODE_ENV=production
PORT=3000

# =============================================================================
# Database Configuration (Production)
# =============================================================================
# Use your production database connection strings
# For Supabase: Use connection pooling URL for DATABASE_URL
#                Use direct connection URL for DIRECT_URL (migrations)
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public&sslmode=require
DIRECT_URL=postgresql://user:password@host:5432/database?schema=public&sslmode=require

# =============================================================================
# JWT Authentication (REQUIRED)
# =============================================================================
# ⚠️  CRITICAL: Generate NEW secrets for production (different from dev!)
# Generate secure random strings (minimum 32 characters):
#   openssl rand -base64 32
# Store these securely (e.g., secrets manager, environment variables in hosting platform)
JWT_SECRET=CHANGE-THIS-TO-PRODUCTION-SECRET-MIN-32-CHARS
JWT_REFRESH_SECRET=CHANGE-THIS-TO-PRODUCTION-SECRET-MIN-32-CHARS
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# Google OAuth (REQUIRED if using Google sign-in)
# =============================================================================
# Get production credentials from Google Cloud Console
# Ensure redirect URIs include your production domain
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Use production Supabase project credentials
# ⚠️  Keep SUPABASE_SERVICE_ROLE_KEY secret - it has admin access!
SUPABASE_URL=https://your-production-project.supabase.co
SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key
